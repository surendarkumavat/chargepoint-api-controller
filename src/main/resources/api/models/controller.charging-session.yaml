---
  components:
    schemas:
      ChargingSessionAck:
        type: object
        properties:
          status:
            description: "Acknowledgment Status."
            type: string
            enum:
              - accepted
            example: accepted
          message:
            description: "Acknowledgment message."
            type: string
            example: "Request is being processed asynchronously. The result will be sent to the provided callback URL."